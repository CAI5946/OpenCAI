{
  "id": "retry_name_cleanup",
  "title": "Normalize names beyond simple trimming",
  "category": "bugfix",
  "fixture": "fixtures/retry_name_cleanup",
  "task": "Fix the failing name normalization tests. First run python -m unittest, inspect the relevant file, apply the smallest patch, rerun python -m unittest, and only finish after tests pass.",
  "verification_command": "python -m unittest",
  "expected_changed_files": ["names.py"],
  "primary_capability": "retry_after_failure",
  "failure_tags": ["partial_fix", "verification_not_run", "patch_failed", "stopped_early"],
  "max_steps": 10,
  "tags": ["level-1a", "micro", "bugfix", "retry"]
}
