{
  "id": "cli_repeat_flag",
  "title": "Add repeat count CLI flag",
  "category": "cli_feature",
  "fixture": "fixtures/cli_repeat_flag",
  "task": "Add the missing --times flag to app.py. The flag should repeat the selected word that many times separated by spaces while preserving the existing default behavior. First run python -m unittest, make the smallest code change, rerun python -m unittest, and only finish after tests pass.",
  "verification_command": "python -m unittest",
  "expected_changed_files": ["app.py"],
  "primary_capability": "cli_behavior_change",
  "failure_tags": ["context_miss", "patch_failed", "wrong_changed_files", "stopped_early"],
  "max_steps": 10,
  "tags": ["level-1a", "micro", "cli", "feature"]
}
