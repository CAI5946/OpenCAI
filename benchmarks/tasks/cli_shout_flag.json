{
  "id": "cli_shout_flag",
  "title": "Add a small CLI flag",
  "fixture": "fixtures/cli_shout_flag",
  "task": "Add the missing --shout flag to app.py. The flag should uppercase the greeting output when present. First run python -m unittest, make the smallest code change, rerun python -m unittest, and only finish after tests pass.",
  "verification_command": "python -m unittest",
  "max_steps": 8,
  "tags": ["micro", "cli", "feature"]
}
